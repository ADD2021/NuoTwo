<UserControl x:Class="ZzaDesktop.OrderPrep.OrderPrepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZzaDesktop.OrderPrep"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Readings"  />
        <DataGrid ItemsSource="{Binding Orders}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding CreatedAt}"
                                    Width="*" />
                
                <DataGridTextColumn Binding="{Binding CO2Consumption}"
                                    Width="*" />

                <DataGridTextColumn Binding="{Binding O2production}"
                                    Width="*" />


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
